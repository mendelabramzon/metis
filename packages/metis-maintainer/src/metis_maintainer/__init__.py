"""metis-maintainer: background intelligence over memory and evidence.

Stage 5 implements the memory core under :mod:`metis_maintainer.memory` — the
``Consolidator`` and the MemCell/MemScene/Profile/Foresight builders. The background
scheduler that drives them on a cadence arrives in Stage 6. May import ``metis_protocol``
and ``metis_core`` only.
"""

__version__ = "0.0.0"
