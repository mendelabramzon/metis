name: writer
version: "1.0.0"
description: Write a file into the scratch dir (for artifact-capture tests).
category: data_cleanup
permissions: []
network: false
sensitivity_ceiling: internal
requires_approval: false
timeout_seconds: 10
max_output_bytes: 65536
