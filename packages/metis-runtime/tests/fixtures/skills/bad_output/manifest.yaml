name: bad_output
version: "1.0.0"
description: Returns output that violates its declared output schema (for schema tests).
category: other
permissions: []
network: false
sensitivity_ceiling: internal
requires_approval: false
timeout_seconds: 10
max_output_bytes: 65536
