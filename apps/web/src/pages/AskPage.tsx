import { SectionPlaceholder } from "./SectionPlaceholder";

export function AskPage() {
  return (
    <SectionPlaceholder
      title="Ask"
      lede="Ask the workspace for a grounded, cited answer. Evidence, scope, and routing show in place — never a black-box reply."
      comingIn="M2 (epic D)"
    />
  );
}
