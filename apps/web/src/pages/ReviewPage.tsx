import { SectionPlaceholder } from "./SectionPlaceholder";

export function ReviewPage() {
  return (
    <SectionPlaceholder
      title="Review"
      lede="One queue for approvals and contradictions. Decide from the source evidence; resolved items leave the pending queue."
      comingIn="M5 (epic F)"
    />
  );
}
