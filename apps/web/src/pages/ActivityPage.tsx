import { SectionPlaceholder } from "./SectionPlaceholder";

export function ActivityPage() {
  return (
    <SectionPlaceholder
      title="Activity"
      lede="A timeline of what happened in workspaces you can access, in plain language — your actions and events, not operator-level audit."
      comingIn="M5 (epic F)"
    />
  );
}
