import { SectionPlaceholder } from "./SectionPlaceholder";

export function SettingsPage() {
  return (
    <SectionPlaceholder
      title="Settings"
      lede="Workspace, members, permissions, model policy, and data & erasure. Operator-only Operations gates further inside."
      comingIn="M5 (epic G)"
    />
  );
}
