import { SectionPlaceholder } from "./SectionPlaceholder";

export function SourcesPage() {
  return (
    <SectionPlaceholder
      title="Sources"
      lede="Connect mailboxes, drives, and chats, or upload documents. Each source shows its health, scope, and sync state."
      comingIn="M2 (epic E)"
    />
  );
}
