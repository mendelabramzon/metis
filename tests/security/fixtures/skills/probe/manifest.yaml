name: probe
version: "1.0.0"
description: Report whether it can see the secret (no permissions).
category: other
permissions: []
network: false
sensitivity_ceiling: internal
requires_approval: false
timeout_seconds: 10
max_output_bytes: 65536
