name: probe_secret
version: "1.0.0"
description: Report whether it can see the secret (secrets permission granted).
category: other
permissions:
  - secrets
network: false
sensitivity_ceiling: internal
requires_approval: false
timeout_seconds: 10
max_output_bytes: 65536
