name: echo
version: "1.0.0"
description: Echo the message back (no approval needed).
category: other
permissions: []
network: false
sensitivity_ceiling: internal
requires_approval: false
timeout_seconds: 10
max_output_bytes: 65536
